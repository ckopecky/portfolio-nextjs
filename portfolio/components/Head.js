import Head from 'next/head';

const DocHead = () => {
	return (
		<Head>
			<title>Christina Kopecky Blog Posts</title>
			<link rel='icon' href='/favicon.ico' />
		</Head>
	);
};

export default DocHead;
